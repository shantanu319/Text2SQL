SELECT YEAR FROM traffic_accidents GROUP BY YEAR ORDER BY COUNT(*) DESC LIMIT 2; SELECT DISTINCT YEAR FROM traffic_accidents WHERE CAUSE NOT IN (SELECT CAUSE FROM traffic_accidents GROUP BY CAUSE HAVING COUNT(*) = 1);